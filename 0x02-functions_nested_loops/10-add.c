#ifndef _PUTCHAR
#define _PUTCHAR
#include "main.h"
#endif

/**
  * add - function to add two int
  * @a: first arg
  * @b: second arg
  * Return: Depends on the arguments
  */
int add(int a, int b)
{
	return (a + b);
}
